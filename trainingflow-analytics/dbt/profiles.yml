trainingflow_databricks:
  target: dev
  outputs:
    dev:
      type: databricks
      catalog: workspace
      schema: trainingflow_gold
      host: "{{ env_var('DATABRICKS_HOST') }}"
      http_path: "{{ env_var('DATABRICKS_HTTP_PATH') }}"
      token: "{{ env_var('DATABRICKS_TOKEN') }}"
      threads: 4
